apiVersion: operations.kubeblocks.io/v1alpha1
kind: OpsRequest
metadata:
  name: surreal-start
  namespace: demo
spec:
  # Specifies the name of the Cluster resource that this operation is targeting.
  clusterName: surreal-cluster
  type: Start
